# Harpoon Story Engine — Environment Variables
# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

# ── AWS S3 ────────────────────────────────────────────────────────
# S3 bucket for rendered publications and uploaded assets
S3_BUCKET=harpoon-stories

# AWS credentials with S3 read/write access
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=eu-west-2

# ── Delivery ──────────────────────────────────────────────────────
# Public domain served by CloudFront (used to construct asset URLs)
DELIVERY_DOMAIN=stories.har.pn
